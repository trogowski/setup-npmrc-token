{
  "name": "setup-npmrc-token",
  "version": "0.0.3",
  "description": "Setup user NPM configuration from template",
  "main": "dist/index.js",
  "bin": "dist/index.js",
  "scripts": {
    "build": "tsc -p . && chmod +x dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tim Rogowski",
  "license": "MIT",
  "types": "dist/index.d.ts",
  "dependencies": {
    "minimist": "^1.2.0",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.0",
    "@types/node": "^13.7.1",
    "typescript": "^3.7.5"
  }
}
